name: Custom Issue Template
description: A template for creating issues with specific fields.
title: "[Category] Title"
labels: ''

body:
  - type: input
    id: title
    attributes:
      name: Title
      description: Enter the title of the issue.
      placeholder: "Issue title"
    validations:
      required: true

  - type: dropdown
    id: category
    attributes:
      name: Category
      description: Select the category of the issue.
      options:
        - base
        - L2
        - L3
        - other
    validations:
      required: true

  - type: textarea
    id: introduction
    attributes:
      name: Introduction
      description: Provide a brief introduction (2 to 3 lines).
      placeholder: "A short description of the issue."
    validations:
      required: true
      minLength: 10
      maxLength: 300

  - type: input
    id: version
    attributes:
      name: Version
      description: Specify the version.
      placeholder: "v1.0.0"
    validations:
      required: true

  - type: dropdown
    id: branch
    attributes:
      name: Branch
      description: Select the branch.
      options:
        - main
        - develop
        - feature/branch-name
        # Add more branches as needed.
    validations:
      required: true

  - type: markdown
    id: description
    attributes:
      name: Description
      description: Provide a detailed description (supports Markdown).
      value: "## Description \n\n"
    validations:
      required: true

  - type: dropdown
    id: mode
    attributes:
      name: Mode
      description: Select the mode.
      options:
        - working
        - milestone
        - technical-correction
        - withdrawal
    validations:
      required: true

  - type: dropdown
    id: first
    attributes:
      name: First
      description: Is this the first time?
      options:
        - "true"
        - "false"
    validations:
      required: true

  - type: input
    id: path
    attributes:
      name: Path
      description: Enter the URL path.
      placeholder: "https://example.com/path"
    validations:
      required: true
      pattern: "^https?://"

  - type: input
    id: ci-build
    attributes:
      name: CI Build
      description: Enter the CI build URL.
      placeholder: "https://ci.example.com/build"
    validations:
      required: true
      pattern: "^https?://"

  - type: dropdown
    id: status
    attributes:
      name: Status
      description: Select the current status.
      options:
        - release
        - trial-use
        - update
        - qa-preview
        - ballot
        - draft
        - normative+trial-use
        - normative
        - informative
    validations:
      required: true

  - type: input
    id: sequence
    attributes:
      name: Sequence
      description: Prepopulated text for the sequence.
      value: "Releases"
    validations:
      required: true
